{
  "name": "Rails 8 + Postgres",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "forwardPorts": [3000, 5432],
  "postCreateCommand": "bundle install && sudo service postgresql start && sudo -u postgres psql -c \"ALTER USER postgres PASSWORD 'password';\" && bin/setup"
}
